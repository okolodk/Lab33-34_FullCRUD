# NotesApp - API для управления заметками по категориям

## Информация о студенте
**ФИО:** Алексеев Григорий 
**Группа:** Исп-232  
**Лабораторная работа:** №33-34  
**Тема:** Полноценный CRUD с базой данных

---

## Описание проекта

NotesApp — это ASP.NET Core Web API приложение для управления заметками с категоризацией. Приложение позволяет создавать, читать, обновлять и удалять заметки, распределяя их по категориям (Работа, Личное, Учёба и др.).

### Основные возможности:
- **Управление категориями**: создание, редактирование, удаление категорий с валидацией
- **Управление заметками**: полный CRUD операций с заметками
- **Фильтрация и поиск**: фильтрация заметок по категории, приоритету, статусу закрепления/архивации
- **Пагинация**: поддержка постраничной загрузки заметок
- **Связи данных**: каждая заметка принадлежит одной категории, категория может содержать много заметок
- **Валидация**: автоматическая проверка данных с помощью Data Annotations
- **RESTful API**: соответствие принципам REST архитектуры



---

## Структура проекта
NotesApp/
├── Controllers/
│ ├── CategoriesController.cs # Контроллер для работы с категориями
│ └── NotesController.cs # Контроллер для работы с заметками
├── Data/
│ └── AppDbContext.cs # Контекст базы данных EF Core
├── Helpers/
│ ├── ApiResponse.cs # Обёртка для успешных ответов
│ └── ApiError.cs # Обёртка для ответов об ошибках
├── Models/
│ ├── Category.cs # Модель категории
│ ├── Note.cs # Модель заметки
│ └── DTOs/
│ ├── CategoryDtos.cs # DTO для категорий (Create, Update, Response)
│ ├── NoteDtos.cs # DTO для заметок (Create, Update, Response)
│ └── NoteFilterDto.cs # DTO для фильтрации заметок
├── Repositories/
│ ├── ICategoryRepository.cs # Интерфейс репозитория категорий
│ ├── CategoryRepository.cs # Реализация репозитория категорий
│ ├── INoteRepository.cs # Интерфейс репозитория заметок
│ └── NoteRepository.cs # Реализация репозитория заметок
├── Program.cs # Точка входа и конфигурация приложения
├── appsettings.json # Файл конфигурации
└── NotesApp.csproj # Файл проекта


---

## Маршруты API

### Категории (Categories)

| Метод | URL | Описание | Коды ответа |
|-------|-----|----------|-------------|
| GET | `/api/categories` | Получить все категории с количеством заметок | 200 |
| GET | `/api/categories/{id}` | Получить одну категорию по ID | 200, 404 |
| GET | `/api/categories/{id}/notes` | Получить категорию со списком заметок | 200, 404 |
| POST | `/api/categories` | Создать новую категорию | 201, 400 |
| PUT | `/api/categories/{id}` | Обновить существующую категорию | 200, 400, 404 |
| DELETE | `/api/categories/{id}` | Удалить категорию | 204, 400, 404 |

### Заметки (Notes)

| Метод | URL | Описание | Коды ответа |
|-------|-----|----------|-------------|
| GET | `/api/notes` | Получить все заметки с фильтрами | 200 |
| GET | `/api/notes/{id}` | Получить одну заметку по ID | 200, 404 |
| POST | `/api/notes` | Создать новую заметку | 201, 400 |
| PUT | `/api/notes/{id}` | Обновить существующую заметку | 200, 400, 404 |
| PATCH | `/api/notes/{id}/pin` | Закрепить/открепить заметку | 200, 404 |
| PATCH | `/api/notes/{id}/archive` | Архивировать/восстановить заметку | 200, 404 |
| DELETE | `/api/notes/{id}` | Удалить заметку | 204, 404 |

### Параметры фильтрации для GET /api/notes

| Параметр | Тип | Описание | Значение по умолчанию |
|----------|-----|----------|----------------------|
| categoryId | int? | Фильтр по ID категории | null |
| isPinned | bool? | Только закреплённые заметки | null |
| archived | bool | Включать архивные заметки | false |
| search | string? | Поиск по заголовку и содержимому | null |
| minPriority | int? | Минимальный приоритет (1-5) | null |
| sortBy | string | Поле сортировки (title, priority, updatedAt, createdAt) | "createdAt" |
| descending | bool | Сортировка по убыванию | true |
| page | int | Номер страницы | 1 |
| pageSize | int | Размер страницы (1-50) | 10 |

---

## Паттерн Repository

### Что это такое?

**Repository (Репозиторий)** — это паттерн проектирования, который создаёт абстракцию между слоем бизнес-логики (контроллеры) и слоем доступа к данным (база данных).

### Архитектура без Repository:
Вся логика работы с БД находится непосредственно в контроллерах.

### Архитектура с Repository:
Логика работы с данными инкапсулирована в репозиториях.

### Зачем он нужен?

| Проблема без Repository | Решение с Repository |
|------------------------|---------------------|
| **Логика запросов размазана** по контроллерам | Все запросы к БД находятся в одном месте (репозитории) |
| **Трудно тестировать** контроллеры отдельно от БД | Можно подменить репозиторий на тестовый (mock) |
| **Дублирование кода** при одинаковых запросах | Переиспользуемые методы в репозитории |
| **Сложная замена БД** — нужно менять все контроллеры | Меняется только реализация репозитория |
| **Контроллеры знают** о структуре БД | Контроллеры работают с абстракцией (интерфейсом) |

### Аналогия из реальной жизни

Представьте склад (база данных) и кладовщика (Repository). Продавцы (контроллеры) не ходят на склад сами — они отправляют запрос кладовщику. Кладовщик знает, где всё лежит и как правильно работать со складом.

### Реализация в проекте

#### Интерфейс (контракт):
```csharp
public interface ICategoryRepository {
    Task<IEnumerable<CategoryResponseDto>> GetAllAsync();
    Task<Category?> GetByIdAsync(int id);
    Task<Category> CreateAsync(Category category);
    // ... другие методы
}
```
## Основные выводы
1. Паттерн Repository — не лишняя абстракция, а способ держать код управляемым при росте проекта
2. Data Annotations решают двойную задачу: валидируют входные данные и задают ограничения в БД
3. Единый формат ответа (ApiResponse) упрощает жизнь фронтенду — он всегда знает, что ждать от сервера
4. DeleteBehavior.Restrict защищает данные от случайного каскадного удаления
5. Include() и проекция в DTO через Select() — правильный способ получить связанные данные без N+1 проблемы